version: '3'services:  flask-app:    build:      context: .      dockerfile: Dockerfile    container_name: voucher-app-container    ports:      - "5000:5000"    volumes:      - ./api:/app/api      - ./data:/app/data    depends_on:      - db  db:    image: "nouchka/sqlite3:latest"    container_name: voucher-sqlite-container    stdin_open: true    tty: true    volumes:      - ./data:/datavolumes:  data: