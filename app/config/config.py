import osclass Config:        SECRET_KEY = 'mysecretkey'    DEBUG = True  # Set to False in production    SQLALCHEMY_TRACK_MODIFICATIONS = False  # Disable SQLAlchemy modification tracking    # Get the absolute path of the script's directory        script_directory = os.path.abspath(os.path.dirname(__file__))        # Navigate one level up to the parent directory and then to the 'data' folder    database_path = os.path.join(script_directory, '..', 'data', 'db.sqlite')    SQLALCHEMY_DATABASE_URI = database_path  # SQLite database URI# Note: Adjust the database URI based on your project's structure and preferencesdef get_config():    return Config